# Dev / test dependencies. Install with:  pip install -r requirements-dev.txt
# Pulls in the pinned runtime deps too: the suite exercises the real pypdf /
# Pillow / openpyxl code paths (PDF-analysis fixtures, proof-log round-trips),
# so pytest alone is no longer enough.
-r requirements.txt
pytest==9.*
